export enum CommonValue {
  ALL = -1,
}
export enum DebounceTime {
  DEFAULT = 500,
}

export enum QueryKey {
  GET_PROFILE = "GET_PROFILE",
  GET_LIST_STAFFS = "GET_LIST_STAFFS",
}

export enum CommonStatus {
  ACTIVE = 1,
  INACTIVE = 0,
}
